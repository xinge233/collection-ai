# xinge233 学习笔记
## 前言
由于这个笔记制作时间较晚,知识并不完整，主要记载了容易遗忘或者常用的知识点，知识主要来源于[鱼C的网课](https://www.bilibili.com/video/BV1c4411e77t/?spm_id_from=333.1387.favlist.content.click&vd_source=acfcb2d84be7e598bf0730315bc7db11 "【Python教程】《零基础入门学习Python》最新版（完结撒花🎉）")部分来源于平常实际操作，ai，同时，这篇笔记也会运用于练习 Markdown 的书写规范  
## Markdown 语法  

### 1.特殊符号前加\\实现正常输入  
### 2.换行:在前一行末尾加上两个空格  
### 3.超链接做法```[显示文字](目标 "悬停显示")```  
### 4.一些字体变化  
| 效果 | Markdown写法 | 说明 |
| ---- | ---- | ---- |
| **加粗** | `**文字**` | 最推荐，兼容性好；也可以用`__文字__` |
| *斜体* | `*文字*` | 也可以用`_文字_` |
| ***粗斜体*** | `***文字***` | 加粗+斜体组合 |
| ~~删除线~~ | `~~文字~~` | 划掉文字 |
| `行内等宽代码` | `` `文字` `` | 灰色背景，等宽字体 |
| <u>下划线</u> | `<u>文字</u>` | HTML标签，原生MD不支持 |
| <span style="color:red;">红色文字</span> | `<span style="color:red;">文字</span>` |  
>这份表格为ai生成  

### 5.代码块输入方式  
````
```语言
代码
```
````
### 6.如果想在 ```` ``` ``` ````内输入```` ``` ````可以在外围输入更多的`` ` ``  
如：输入````` ```` ``` ```` `````则会返回```` ``` ````

### 7.文本内跳转

```
<a id="name"></a>#一定要用"a"
这里是我要跳转到的普通文本。

[点击跳转到这段文字](#name)
```
>根据ai,如果没有</a>系统会自动修复，但可能会出问题

即  
<!-- 在目标位置放锚点标签 -->
<a id="name"></a>
这里是我要跳转到的普通文本。

<!-- 在别的地方写跳转链接 -->
[点击跳转到这段文字](#name)

>此处为ai生成

### 8.强制换行  
可以使用 \<br> 进行强制换行，在表格中写代码块有用
## python一些使用代码块  
### 1.输入以空格分离的整数
```python  
a,b=map(int,input().split)  
```

  

### 2.try语句用法   
try用于在报错时能运行特定代码
```python
try:
    执行的代码，如:int(混合字符串和整型的列表)
except 报错类型:#如果什么都没写就是所有报错类型
    报错时执行的代码
else:
    正常时执行的代码
finally:
    无论怎样都会执行的代码
```

### 3.类型判断
```python
type(检测对象)#返回对象类型
isinstance(检测对象,类型)#返回布尔值
```
#### 核心区别:
type 认精准类型
isinstance 人继承
例子:
```python
class Animal: pass
class Dog(Animal): pass

d = Dog()

type(d) == Animal          # False   ❌ type 只认精确类型
isinstance(d, Animal)      # True    ✅ isinstance 认继承
```
>例子由ai生成
## 知识点归纳

## 列表

### 1.将混合类型的参数输入列表
```python  
enter=input("请输入字符，以空格分离")
lst=[]
for i in enter.split():
    lst.append(i)
```
### 2.将列表中的函数提取(解包)
```python
lst=[1,"b","xyz",3.14]
print(*lst)
```
效果近似于
```python
lst=[1,"b","xyz",3.14]
for i in lst:#使i等于lst中的每一个元素(遍历)
    print(i)#区别在于这样会每行输出一个元素
    #prnt(i,end=" ")#这样使每个元素输出时以空格分割，效果相同
    #print("")换行，使下一次输出会在下一行
```
### 3.切片
```python
lst=[1,"b","xyz",3.14]
new_lst=lst[2:0:-1]#左闭右开，提取了 "xyz" "b"
```
如果是倒着提取，开始和结尾也要进行更改，同样遵循左闭右开  
~~这还是deepseek老师在检验我第一次写错的版本时告诉我的~~
### 4.浅拷贝、深拷贝
**浅拷贝**——只拷贝最外面的那层列表，对于内部的元素只是引用  
**深拷贝**——拷贝每一个列表中的每一个元素，每一个元素都是与原来不同新的元素
#### 浅拷贝
```python
a=[1,2[3,4]]
b1=a.copy()#括号内一定为空
b2=a[:]
b3=list(a)
b4=copy.copy(a)#括号内为拷贝对象
```
>例子由ai生成,注释为手动添加
#### 深拷贝
```python
a=[1,2[3,4]]
import copy
b=copy.deepcopy(a)
```
>根据ai，这是唯一推荐的写法  

#### 注意！:`a = b` 和 `*` 属于引用，不属于深/浅拷贝，a会随着b的值改变而改变，直到***重新赋值***
>“直到***重新赋值***”是ai修正的  
根据ai 有“=”时（除了+=,-+）几乎都为重新赋值  
其他属于原地修改

可通过`a is b `的返回值判断二者是否归属于同一地址  
`print(a is b)`
等价于
```python
if id(a)==id(b):
    print(True)
else:
    print(False)
```
>根据ai修改，直接等价于`id(a)==id(b)`返回的就是布尔值，但是这样写比较直观，所以保留
### 5.列表推导式
 

基本结构`放进列表的元素 for 变量 in 可迭代对象 if 条件`  

```python
 # 普通写法
squares = []
for x in range(5):
    squares.append(x ** 2)
# [0, 1, 4, 9, 16]

# 推导式写法
squares = [x ** 2 for x in range(5)]
# [0, 1, 4, 9, 16]
```
>例子为ai生成

其中`放进列表的元素`
也可以if  
`[默认输出 if 条件 else 满足条件时输出 for 变量 可迭代对象]`
```
["偶" if x % 2 == 0 else "奇" for x in range(5)]
# ['偶', '奇', '偶', '奇', '偶']

[x if x > 0 else 0 for x in [-1, 2, -3, 4]]
# [0, 2, 0, 4]
```
>例子为ai生成

### 多重循环
`[最后输出的元素 for 在前面的元素先进行迭代 in 可迭代对象1 for 放在后面的元素再进行迭代 in 可迭代对象2 ...]`

```python
[(a1,a2,a3) for a1 in range(3) for a2 in range(3) for a3 in range(3)]#先固定 a1, 再固定a2,a3进行迭代,a3迭代好了a2迭代 1 次，再次进行a3的迭代.....a2迭代好了才到a1迭代 1 次.....
```
等价于
```python
result=[]
for a1 in range(3):
    for a2 in range(3):
        for a3 in range(3):
            result.append((a1,a2,a3))
            #这里append后面呢是个元组，append只能传一个元素，要将三个元素()变成一个元组
```
### 其他列表操作

| 操作 | 返回值 | 是否原地 | 说明 |
|---|---|---|---|
| [] / [1,2,3] | 列表 | 新建 | 字面量 |
| list(iterable) | 列表 | 新建 | 建一个表格 <br>***只能建一个*** |
| a[i] | 元素 | — | 索引 |
| a[1:3] / a[::2] / a[::-1] | 列表 | 新建 | 切片 |
| a.append() | None | 原地 | 末尾加一个 |
| a.extend() | None | 原地 | ()内是可迭代对象<br>***如果是字符串的话，则会把每个字符作为元素添加*** |
| a.insert(第一个元素所在的位置, 待插入内容) | None | 原地 | 会将后面的元素往后挤 |
| a += [x] | None | 原地 | 等价 extend |
| a = a + [x] | 列表 | 新建 | 拼接后重新赋值 |
| a.pop() | 被删元素 | 原地 | 删()内的元素，并返回，默认删最后一个元素，()括号内为负数是为倒数  |
| a.remove(x) | None | 原地 | 删第一个值为 x |
| del a[i] / del a[1:3] | 无 | 原地 | 按索引 / 切片删 |
| a.clear() | None | 原地 | 清空 |
| a[i] = x | 无 | 原地 | 改单个 |
| a[1:3] = [...] | 无 | 原地 | 切片赋值，长度可变 |
| a.index(x) | 索引 | — | 第一个 x 的下标***找不到会报错*** |
| a.count(x) | 次数 | — | x 出现次数 |
| x in a | bool | — | 是否包含 |
| len(a) | int | — | 长度 |
| a.sort(key=排序规则,从小到大, reverse=True/False) | None | 原地 | 原地排序 |
| a.reverse() | None | 原地 | 原地反转 |
| for i, x in enumerate(a): | — | — | 输出为<br>`下标 元素` |
| for x, y in zip(a, b): | — | — | 同时遍历多个 |
| tuple(a) / set(a) | 元组 / 集合 | 新建 | 类型转换 |
| sum(a) / max(a) / min(a) | 数值 | — | 对列表内所有元素求和 / 最值 |
| any(a) / all(a) | bool | — | 任一为真 / 全部为真 ,返回`True`否则返回`False`|
>ai生成，经过了人为筛选和修改
## 元组
可通过`tuple(转换对象)`将其他类型转化为元组
### 粗略理解:不可变的列表
 >但可以被新建  

 ### 仅对于元组 `(元组) +=(新的元组)` 会新建个元组，效果仍然是在末尾添加
 ```python
 t = (1, 2)
t += (3, 4)
print(t)         # (1, 2, 3, 4)
```
### 同时元组更快更省内存
### 元组的方法少

由于不可变，只能进行查询，仅可进行`index()`和`cout()`
## 字符串


### 1.字符串相关操作
| 功能 | 代码写法 | 示例 | 说明 |
| ---- | ---- | ---- | ---- |
| 定义字符串 | `s = "hello"` / `s = 'hello'` | `s = 'Python'` | 单引号、双引号都可以；多行用三引号`"""..."""` |
| 取值（下标） | `s[下标]` | `s = "hello"`<br>`print(s[0])` # h | 获取对应下标字符，下标从0开始；`s[-1]`取最后一位 |
| 切片 | `s[start:end:step]` | `s = "hello"`<br>`print(s[1:4])` # ell | 截取子串，左闭右开，end位置取不到 |
| 字符串拼接 | `s1 + s2` | `s1 = "hi"`<br>`s2 = "py"`<br>`print(s1+s2)` # hipy | 两个字符串相加合并 |
| 重复输出 | `s * n` | `s = "hi"`<br>`print(s*3)` # hihihi | 将字符串重复n次 |
| ***判断是否包含*** | `子串 in s` | `s = "hello"`<br>`print("he" in s)` # True | 判断子串是否存在，返回布尔值 |
| 求长度 | `len(s)` | `s = "hello"`<br>`print(len(s))` # 5 | 返回字符串字符个数 |
| ***分割字符串*** | `s.split("分隔符")` | `s = "a b c"`<br>`print(s.split(" "))` # ['a','b','c'] | 按指定符号切分，返回列表；不带参数默认按任意空白分割 ***分割后的结果每一个都是单独的元素*** 这是基础版，下面字符串的截取有完整的|
| ***合并列表为字符串*** | `"连接符".join(lst)` | `lst = ["a","b"]`<br>`print("-".join(lst))` # a-b | 把字符串列表拼接成一个字符串 |
| ***替换内容*** | `s.replace("旧","新")` | `s = "hi hi"`<br>`print(s.replace("hi","hey"))` # hey hey | 将所有旧子串替换为新子串 |
| ***找位置*** | `s.find("寻找目标,开始，结尾")` | `s = "hello"`<br>`print(s.find("el"))` # 1 | 依旧左闭右开，与`index()用法相同`找到返回起始索引，找不到返回-1,可在前面添加`r/l`进行从左往右或从右往左寻找，区别:`index()`找不到会报错 |
| ***去除首尾空格*** | `s.strip()` | `s = "  hi  "`<br>`print(s.strip())` # hi | 去掉首尾空白；`lstrip()`清左侧，`rstrip()`清右侧 |
| ***转大写*** | `s.upper()` | `s = "hi"`<br>`print(s.upper())` # HI | 全部字母转为大写，`isupper`***检测是否全部字母为大写*** |
| ***转小写*** | `s.lower()` | `s = "HI"`<br>`print(s.lower())` # hi | 全部字母转为小写,`islower`***检测是否全部字母为小写*** |
| ***判断是否纯数字*** | `s.isdigit()` | `s = "123"`<br>`print(s.isdigit())` # True | 全部是数字返回True |
| ***判断是否纯字母*** | `s.isalpha()` | `s = "abc"`<br>`print(s.isalpha())` # True | 全部是字母返回True |
|***翻译表***|`规则名=str.maketrans("转换前","转换后","删掉的东西"`<br>`print("应用的字符串".translate(规则名))`|` table=str.maketrans("abcde","12345","l")#a=1 b=2 c=3 d=4 e=5 删掉l ` <br> ` print("jklad".translate(table))#输出jk14 `|***也可以放在同一行，效果相同*** <br> `print("jklac".translate(str.maketrans("abcde","12345")))`|
>表格为ai+手动修改,功能列粗斜体为不熟悉的内容，说明列粗斜体为手动添加的内容
### 2.格式化输出<a id="f_explain"></a>
可以在字符串中引入变量
#### f-string
```python
name = "小明"
print("name")  # 输出 name，不会读取变量
```
```python
name = "小明"
print(f"我的名字是{name}") # 输出：我的名字是小明
```
```python
a = 10
b = 2
print(f"a除以b = {a / b}") #括号内可运算
```
```python
age = 18
print("年龄：" + str(age))#效果相同
```
>例子为ai生成

### %
#### 基本用法
```python
(%占位符 % (占位符显示的文字，用“,”隔开,只有一个时可以去掉括号))
#区别与C++,分离用的","变成了"%"
```
~~不是哥们，你学C++的还不会用%~~  
| 格式符 | 类型 | 示例 | 结果 |
|---|---|---|---|
| `%s` | 字符串（万能） | `"%s" % 123` | `"123"` |
| `%r` | repr（调试用） | `"%r" % "a"` | `"'a'"` |
| `%d` | 十进制整数 | `"%d" % 3.9` | `"3"` |
| `%f` | 浮点数 | `"%f" % 3.14` | `"3.140000"` |
| `%.2f` | 保留两位小数 | `"%.2f" % 3.14159` | `"3.14"` |
| `%x` | 十六进制（小写） | `"%x" % 255` | `"ff"` |
| `%X` | 十六进制（大写） | `"%X" % 255` | `"FF"` |
| `%o` | 八进制 | `"%o" % 8` | `"10"` |
| `%e` | 科学计数法（小写） | `"%e" % 12345` | `"1.234500e+04"` |
| `%E` | 科学计数法（大写） | `"%E" % 12345` | `"1.234500E+04"` |
| `%c` | 字符 | `"%c" % 65` | `"A"` |
| `%%` | 百分号本身 | `"100%%"` | `"100%"` |
| `%10s` | 宽度 10，右对齐 | `"%10s" % "abc"` | `"       abc"` |
| `%-10s` | 宽度 10，左对齐 | `"%-10s" % "abc"` | `"abc       "` |
| `%05d` | 宽度 5，补零 | `"%05d" % 42` | `"00042"` |
| `%+d` | 显示正负号 | `"%+d" % 5` | `"+5"` |
| `%(name)s` | 字典取值 | `"%(name)s" % {"name": "Tom"}` | `"Tom"` |  

>ai生成

总体上和C++类似
#### format
##### 基本用法
```python
"{占位符：格式规范}".format(占位符对应的内容,用法和 % 一样)     
```
#### 格式规范
```python
"{:*^+#015,.2f}".format(12345.6789)#"***+12,345.68**"
```
格式规范顺序固定`{:[填充][对齐][符号][#][0][宽度][,][.精度][类型]} `  
  
`*` 使空位用 `*` 填充，可换成其他字符
  
`^` 使输出居中，居左右分别为 `<` ， `>`
  
`+` 使最前面加+,变为正数
  
`#` 使输出变为进制,由于这里是f类型，所以不生效  
>"{:#x}".format(255)        # "0xff"      十六进制带前缀  
"{:#X}".format(255)        # "0XFF"  
"{:#o}".format(8)          # "0o10"      八进制带前缀  
"{:#b}".format(10)         # "0b1010"    二进制带前缀  
ai生成
  
`15` 控制总宽度，因此在居中是左右两边会产生被 `*` 填补的空位,如果使用 `015` 则会在空位用 `0` 填充，但是这里自定义填充优先

`,` 使其输出变为每千分位一个 `,`
  
`.2` 保留两位小数
  
`f` 输出类型，为浮点数

### 3.字符串检测
| 功能 | 代码写法 | 示例 | 说明 |
| ---- | ---- | ---- | ---- |
| 判断是否全部为数字 | `s.isdigit()` | `s="1234"`<br>`print(s.isdigit())` # True | 纯阿拉伯数字返回True；小数、负号、汉字数字返回False |
| 判断是否全部为字母 | `s.isalpha()` | `s="python"`<br>`print(s.isalpha())` # True | 只能是英文字母，不能包含数字、空格、符号 |
| 判断字母+数字（无符号空格） | `s.isalnum()` | `s="py123"`<br>`print(s.isalnum())` # True | 只有字母和数字，没有空格、标点就返回True |
| 判断是否全部小写字母 | `s.islower()` | `s="hello"`<br>`print(s.islower())` # True | 字符串里字母都是小写，非字母不影响结果 |
| 判断是否全部大写字母 | `s.isupper()` | `s="HELLO"`<br>`print(s.isupper())` # True | 字符串里字母都是大写，非字母不影响结果 |
| 判断是否每个单词首字母大写 | `s.istitle()` | `s="Hello World"`<br>`print(s.istitle())` # True | 单词首字母大写，其余小写 |
| 判断是否只由空白字符组成 | `s.isspace()` | `s="  \t"`<br>`print(s.isspace())` # True | 空格、制表符、换行符都属于空白 |
| 判断是否以指定子串开头 | `s.startswith("子串")` | `s="hello"`<br>`print(s.startswith("he"))` # True | 判断字符串开头是不是目标内容 |
| 判断是否以指定子串结尾 | `s.endswith("子串")` | `s="hello"`<br>`print(s.endswith("lo"))` # True | 判断字符串结尾是不是目标内容 |
|检测是否为 keyword ,即 python已经使用的字符|`import keyword `<br> `keyword.iskeyword(待判断的字符串)`|`import keyword `<br> `keyword.iskeyword("print")`#True|不知道有啥用，先记着
>表格为ai生成+手动修改

### 6.字符的截取
| 功能 | 代码写法 | 示例 | 说明 |
| ---- | ---- | ---- | ---- |
| 取单个字符（按下标） | `s[下标]` | `s="hello"`<br>`print(s[0])` # h | 下标从0开始；`s[-1]`取最后一个字符 |
| 截取 | `s[start:end:step]` | `s="hello"`<br>`print(s[::2])` # hlo | step为间隔，隔几个取一个,左闭右开,结果为列表 |
| 倒序截取 | `s[::-1]` | `s="hello"`<br>`print(s[::-1])` # olleh | step=-1，字符串反转 |
| 从末尾往前截取 | `s[start:end:step]` 负步长 | `s="hello"`<br>`print(s[-3:])` # llo | 负下标从右往左数 |
| 查找子串位置后截取 | `s.find(子串)` 配合切片 | `s="ab-cd"`<br>`print(s[s.find("-")+1:])` # cd | 先找到位置，再切出后面的部分 |
|split截取|`s.split("分隔符",maxsplit=次数)` | `s="a-b-c-d"`<br>`print(s.split("-",maxsplit=2))` # ['a','b','c-d'] | 只切maxsplit次，剩下的不再切割，如果次数为负数，则会尝试分割所有字符，默认为-1，结果为元素 |

## 对序列的函数（部分）
### min()/max()
二者用法一样，故只以max为例
`max(对象,default ="如果对象是空的，就输出这段文字")#如果对象是字符串，则输出编码值最大的字符`
>字符编码值大写小于小写，按26字母递增

### sum()
``sum(s,start=初始值)#输出s的求和+初始值 ``
### sorted()

`sorted()`为新建  
`.sort`为原地修改  
二者用法相同`sorted(s,reverse=True/False,key=排序规则函数)#默认从小到大`
### reversed
`reversed()`返回的值为可迭代对象,需用list()之类的函数转换
### enumerate
`enumerate`输出`下标 内容`,需转化
### zip
`zip(x,y,z)` 把x,y,z依次打包成一个元组,如果长度不一样，则会同一为最短的长度，多余的长度直接截掉，需转化  
#### 如果要输出最长的元素
```python
import itertolls
zipped = itertools.zip_longest(x,y,z)
#输出为最长的元素，其他元素与其差值会用None填补
```
### map
```python
map(操作,操作对象1，操作对象2,操作对象...)
#需转化,面对长度不一的元素方法同zip
```
### fliter
```fliter(要求，对象)#输出符合要求的对象，需转化
```
### iter
将迭代器转化为可迭代对象
>迭代器 可重复使用

>可迭代对象 提取一个少一个
### next
提取下一个可迭代对象
`next(迭代器,"迭代完后说的话")
## 字典  
字典的速度快于列表
```python
字典名 = {键（输入这个）:值（获得这个）}#无法进行切片，下标无序
print(字典名[(键)输入这个])#值(获得这个)
字典名[添加的键]=添加的值 #{键（输入这个）:值（获得这个），添加的键：添加的值}
```
### 字典的创建
```python
a={直接赋值}
b=dict(键=值)#采用函数赋值，键不加引号
c=dict([(键，值)])#用函数将列表转换为字典
d=dict(zip([键][值]))将键和值依次组合
```
>你也可以不同方法混着用，~~不过何意味~~

### 字典的方法
```python
d = dict.fromkey(一堆键，值)#给一堆键赋上相同的值，如果没有值则为None
d[其中一个键] = 值#改变一个键的值
d.pop(键，defult=唐完了显示这个)#弹出一个键与其对应的值，不能为空
d.popitem()弹出(最后一个键，最后一个值)#不能指定
del d[值]#删掉某个键值对，如果没有[]则删除整个字典
d.clear()#将字典变为空字典
d.update({键=值，键=值})#批量修改键对应的值
d.get(键，找不到键时返回的值)
d.setdefault(键，值)#返回键的值，如果键不存在，则在字典中插入键值对并返回
d.keys()#键的视图对象
d.values()#值的视图对象
d.items()#键值对的视图对象
```
>视图对象即读取字典，会随着字典改变而改变
### 其他字典的方法
操作对象默认为键，可使用`d.values`或`d.items`改变操作对象
```python
list()
len()
next()
iter()
```
### 字典的嵌套
```python
d = {键1：{键2：值2},[1,2,3]}
d[键1][键2] #返回 值2
d[键1][1] 返回2
```
### 字典推导式
用法和列表推导式差不多,可对单独对键和值进行推导,也有不一样的用法
```python
d = {"a": 1, "b": 2, "c": 3}

reversed_dict = {v: k for k, v in d.items()}
print(reversed_dict)
# 输出：{1: 'a', 2: 'b', 3: 'c'}
#实现对键值对的改变
```
>例子由ai生成
## 集合
集合中所有元素都独一无二且无序
集合的运行效率远快于列表，但代价是要占用更多的存储空间
`集合名 ={集合内容}`
`set()#使用set函数转化为集合`
### 集合的方法
```python
a.copy()
a.add()#将()内的内容整体加入集合
a.remove()#删除某个元素，会报错
a.discard()#删除某个元素，不会报错
a.pop()#根据随机顺序弹出元素
a.clear()#清空集合
a.isdisjoint(b)#判断a和b是否有交集，返回布尔值
a.issubset(b)#判断a是否为b的子集
a.issuperset(b)判断a是否为b的超集,即a的所有元素是否都在b中
a.union(b)#取a和b的并集，返回并集
a.intersection(b)#取ab交集，可在改为intersection_update更新原集合内容
a.difference(b)#取a和b的差集,即将b作为全集，取a的补集可添加_update
a.symmetric_difference(b)#取对称差集,即取a并b - a交b可添加_update
a <=b #检测子集
a < b #检测真子集
a >= b #检测超集
a > b #检测真超集
a | b | c #取并集
a & b & c #取交集
a - b - c #取差集
a ^ b ^ c #取对称差集
```
方法可以使用任意类型，而运算符只能用集合
### 不可变集合
集合分为可变`set()`和不可变`frozenset()`，默认为可变  
对不可变结合update会报错
### 可哈希
`hash()`可获得某个元素的哈希值
他说这里之后再研究  
反正就是说字典不可变，可哈希，列表可变,不可哈希
因此set集合不可嵌套，嵌套需用frozenset集合
## 函数
```python
def 自定义函数名(参数1，参数2):#这里的参数被称为形式参数
    函数的具体内容
    return 返回参数#执行函数后返回的值并直接结束函数的执行，如果咩有可不写return
自定义函数名（参数1，参数2）#调用函数，这里的参数被称为位置参数
```
在下次使用只需输入`自定义函数名(自定义参数)`就可以执行对应代码块
默认情况在使用函数用的参数依次为函数定义时的顺序，但可特定参数
```python
def fx =(x1,x2,x3)
fx(a,b,c)#此时a对应x1，b对应x2，c对应x3
fx(x3=a,x1=b,x2=3)#此时对应关系发生改变，这里的参数叫做关键词参数
位置参数和关键词参数可混用,但位置参数必须在关键词参数之前
```python
greet("Tom", age=20)          # ✅
greet(name="Tom", 20)         # ❌ 语法错误
```
>例子由ai生成
在定义函数时，也可定义默认参数，当默认参数不被调用时，就会使用默认参数
```python
def greet(name, greeting="你好"):
    print(f"{greeting}，{name}")

greet("Tom")                  # 你好，Tom      （用默认值）
greet("Tom", "早上好")         # 早上好，Tom    （传了就用传的）
greet("Tom", greeting="嗨")    # 嗨，Tom       （关键字传）
```
>例子为ai生成
可以在定义函数时在()内插入`/`,`/`左侧只能使用位置参数,右侧可以  
可以在定义函数时在()内插入`*`,`*`右侧只能用关键词参数，左侧可以
```python
def f(a, b, /, c, d, *, e, f):
    print(a, b, c, d, e, f)
f(1, 2, 3, 4, e=5, f=6)          # ✅
f(1, 2, 3, d=4, e=5, f=6)        # ✅
f(1, 2, c=3, d=4, e=5, f=6)      # ✅
f(a=1, b=2, c=3, d=4, e=5, f=6)  # ❌ a、b 不能关键字
f(1, 2, 3, 4, 5, 6)              # ❌ e、f 必须关键字
```
可以在参数前加个`*`是其成为收集参数,即多个参数打包成一个元组
如果参数前为`**`则会将吃多个参数打包成一个字典
在元组前加`*`可解包元组
在字典前加`**`可解包字典的值
z
```python
def func(**dictionary):
func(a=1,b=2,c=3)#{'a':1,'b'=2,'c'=3}

```python
def fx(*args)
fx(1,2,3,4)#参数为(1,2,3,4)
```
当收集参数和形式参数混用时，形式参数只能用关键词参数代入  
`*`就是一个匿名的收集参数
`global x`可以在函数中对全局变量进行操作
函数也可进行嵌套，但是被嵌套的函数无法被直接调用  
若想先调用被嵌套的函数，需先调用外层的函数
可使用`nonlocal x`使内层对`x`的修改可影响外层函数
| L | E | G | B |  
|---|---|---|---|
|Local|Enclosed|Global|Build-In|
|局部作用域|嵌套函数外层作用域|全局作用域|内置作用域|
|当前嵌套层中的作用域|外面的嵌套层的作用域|全局的作用域|内置的作用域|

最左边的覆盖右边的L>E>G>B  
可以通过`funA()()`实现对内层函数的调用
或者
```python
def FA(A):
    FB(B):
        return FB
C=FA(x)#这里是对第一层赋值
C(y)#这里是对第二层赋值
```
函数也可被函数调用
```python
def func1():
def func2(func):
    func()
func2(func1)
```
### 修饰器
```python
def FA(FB):
    def FC():
        FB()
    return FC 
@FA
def FD():
```
将FD作为FB（括号内的函数）代入到FA里面，最后将调用FD的内部函数FC（返回的函数）定义为新的FD  
  
装饰器也可以一次性调用多个，会***从下往上***执行  
```python
def FA(FB):
    def FX():
        FB()
    return FX
def FC(FB):
    def FX():
        FB()
    return FX
def FD(FB):
    def FX():
        FB()
    return FX
@FA#最后执行这个
@FC#再执行这个
@FD#先执行这个
def FE():
```
由于FX和FB都属于局部变量，在定义时离开作用域时局部变量会被消除，故可以重复使用  
  
装饰器也可以传入参数
```python
def FA(FX):
    def FB(FY):
        def FC():
            print(FX)
            FY()  
        return FC
    return FB
@FA(FX=x)
def FD()
```
FA的效果为定义FB,最后返回FB作为最终结果  
FB的效果为定义FC,最后返回FC到FA  
FC的效果为代入前面两层引入的函数,并执行内容(print和FY) 
@FA由于已经使用(FX=x)定义了第一层FX的内容,后面的FD代入的是第二层FY的内容  
### Lambda 表达式
  
乃运用于简单之关系函数，无序为其命名的「匿名函数」*~~有点中二~~*
```python
squareX=lambda x(形式参数) : x*x(函数内容)`  
```
等同于
```python
 def squareX(x):
    return x**2
```

可以理解为f(x)=x*x
表达式整体为一个元素，故可以放入列表等
```python
lst=[lambda x:x**2,2,3]
lst[0](lst[1])#提取lst[0],即lambda表达式,并将lst中的[1],即2代入到表达式中
#输出4
```

